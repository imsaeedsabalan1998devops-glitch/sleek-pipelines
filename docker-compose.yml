version: "3.9"

services:
  app:
    build: .
    container_name: devops_playground
    command: python src/main.py
    volumes:
      - .:/app
    ports:
      - "${APP_PORT:-8000}:${APP_PORT:-8000}"
    environment:
      - APP_PORT=8000
      - APP_LOG_LEVEL=INFO
